# 电子秤模拟测试指南

## 🎯 概述

本指南说明如何使用XCOM软件模拟电子秤数据，并在Windows版本的烟草称重系统中实时显示重量数据。

## 📋 系统架构

```
XCOM (COM4) → 虚拟串口(COM3↔COM4) → 烟草称重系统 (COM3)
     ↓              ↓                    ↓
发送模拟数据    数据传输通道        接收并显示数据
```

## 🛠️ 环境准备

### 1. 安装com0com虚拟串口软件
- 下载并安装com0com
- 创建COM3↔COM4虚拟串口对
- 确保串口对状态为"Enabled"

### 2. 配置XCOM V2.6
- 串口选择：COM4
- 波特率：9600
- 数据位：8
- 停止位：1
- 校验位：无

### 3. 准备测试数据
在XCOM发送区域输入以下电子秤模拟数据：
```
ST, NT, + 0.00kg
ST, NT, + 1.25kg
ST, NT, + 2.50kg
ST, NT, + 3.75kg
ST, NT, + 5.00kg
ST, NT, + 6.25kg
ST, NT, + 7.50kg
ST, NT, + 8.75kg
ST, NT, + 10.00kg
```

## 🚀 测试步骤

### 步骤1：启动虚拟串口
1. 打开com0com Setupc程序
2. 确认COM3↔COM4串口对已创建并启用
3. 点击"Apply"应用设置

### 步骤2：配置XCOM
1. 打开XCOM V2.6
2. 串口选择：COM4
3. 波特率：9600
4. 在发送区域输入测试数据
5. 勾选"定时发送"，周期：500ms
6. 勾选"发送新行"
7. 点击"打开串口"

### 步骤3：运行测试程序
1. 双击运行 `test-scale-data.bat`
2. 观察控制台输出
3. 确认接收到XCOM发送的数据

### 步骤4：运行完整系统
1. 运行烟草称重系统主程序
2. 观察UI界面中的重量显示
3. 验证重量数据实时更新

## 📊 数据格式说明

### 电子秤数据格式
```
ST, NT, + 0.00kg
│  │   │  │
│  │   │  └─ 重量值（kg）
│  │   └──── 符号（+/-）
│  └──────── 类型（NT=净重）
└─────────── 状态（ST=稳定，US=不稳定）
```

### 解析逻辑
- **状态标识**：ST=稳定，US=不稳定
- **类型标识**：NT=净重
- **重量提取**：从"+ 0.00kg"中提取数值部分
- **稳定性判断**：根据状态标识判断

## 🔧 代码修改说明

### 1. ScaleManager.java
- 修改了`parseWeightData`方法，支持解析"ST, NT, + 0.00kg"格式
- 添加了`parseScaleData`方法，专门解析电子秤协议
- 添加了`tryParseOtherFormats`方法，支持多种数据格式

### 2. ScaleData.java
- 新增数据模型类，存储解析后的电子秤数据
- 包含重量、稳定性、状态、类型等信息

### 3. MainController.java
- 修改了`updateCurrentWeight`方法，增强UI显示
- 添加了重量变化时的状态更新
- 根据重量值自动启用/禁用确认按钮

## ⚠️ 故障排除

### 问题1：串口无法连接
**症状**：程序显示"电子秤未连接"
**解决方法**：
1. 检查com0com串口对是否启用
2. 确认XCOM已打开串口
3. 检查串口参数是否匹配

### 问题2：数据无法接收
**症状**：程序连接成功但无数据
**解决方法**：
1. 确认XCOM正在发送数据
2. 检查数据格式是否正确
3. 查看程序日志输出

### 问题3：数据解析错误
**症状**：接收到数据但解析失败
**解决方法**：
1. 检查XCOM发送的数据格式
2. 确认数据包含必要的分隔符
3. 查看解析日志

## 📈 测试场景

### 场景1：基础重量测试
- 发送固定重量数据
- 验证UI显示正确

### 场景2：重量变化测试
- 发送递增的重量数据
- 验证UI实时更新

### 场景3：稳定性测试
- 发送ST（稳定）和US（不稳定）状态
- 验证状态显示逻辑

### 场景4：异常数据测试
- 发送错误格式数据
- 验证错误处理逻辑

## 🎉 成功标准

当系统正常工作时，你应该看到：

1. **程序启动**：显示"电子秤连接成功"
2. **数据接收**：控制台显示接收到的原始数据
3. **数据解析**：显示解析后的重量值
4. **UI更新**：界面中的重量标签实时更新
5. **状态变化**：根据重量值自动启用/禁用按钮

## 📝 注意事项

1. **串口独占**：确保没有其他程序占用COM3
2. **数据格式**：严格按照"ST, NT, + 0.00kg"格式发送
3. **发送频率**：建议500-1000ms间隔，模拟真实电子秤
4. **错误处理**：程序会自动处理格式错误的数据

## 🔄 下一步

成功测试后，你可以：
1. 集成到完整的烟草称重系统
2. 添加更多电子秤协议支持
3. 实现重量稳定性检测
4. 添加数据记录和统计功能 