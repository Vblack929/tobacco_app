# 身份证读卡器模拟测试指南

## 概述
本指南介绍如何使用模拟HTTP服务器测试身份证读卡器功能，无需物理硬件设备。

## 快速开始

### 1. 启动模拟服务器
```bash
# 进入项目目录
cd /path/to/tobacco_app

# 启动模拟服务器（监听端口7846）
java -cp "target/classes:lib/*" com.tobacco.weight.test.SimpleMockServer
```

### 2. 启动主应用程序
```bash
# 在另一个终端中启动主应用程序
mvn compile exec:java -Dexec.mainClass="com.tobacco.weight.ui.TobaccoWeightApp"
```

## 模拟服务器功能

### 测试数据
模拟服务器返回真实的农户数据：
- **姓名**: 伍伟亮, 伍伟林, 伍丰虎
- **身份证号**: 真实格式 (例如: 430123****3478)
- **提取数据**: 从身份证号中提取生日和性别
- **随机地址**: 真实的中国地址

### 可用接口
- `GET /getReaderInfo` - 设备信息
- `GET /api/readCard` - 模拟身份证读取
- `GET /readSamID` - SAM模块ID
- `GET /readCardNo` - 仅读取卡号

### 响应格式
```json
{
  "errorMsg": "OK",
  "resultContent": {
    "name": "伍伟亮",
    "idNum": "43012319801123478X",
    "gender": "1",
    "birthday": "19801123",
    "address": "湖南省长沙市芙蓉区...",
    "photo": "base64_encoded_image"
  },
  "resultFlag": "0",
  "status": "0"
}
```

## 测试流程

### 1. 检查连接状态
- **绿色圆点 (●)**: 已连接到模拟服务器
- **红色圆点 (●)**: 未连接
- 如需要可使用 **"重连"** 按钮

### 2. 测试身份证读取
1. 点击 **"读取身份证"** 按钮
2. 等待模拟延迟 (800-1300毫秒)
3. 农户信息自动填充
4. 从数据库获取合同信息

### 3. 手动测试
- 手动输入模拟数据中的身份证号
- 测试数据库查询功能
- 验证行为一致性

## 故障排除

### 模拟服务器无法启动
- 检查端口7846是否被占用
- 确保Java类路径包含所需的JAR文件

### 应用程序无法连接
- 验证模拟服务器正在运行 (`curl http://127.0.0.1:7846/getReaderInfo`)
- 在应用中点击 **"重连"** 按钮
- 检查防火墙设置

### 无数据返回
- 模拟服务器随机选择测试农户
- 尝试多次读取操作
- 检查服务器控制台日志

## 开发说明

- 模拟服务器模拟真实的时间延迟
- 生日/性别从身份证号格式中提取
- 照片数据是base64占位符
- 所有合同号来自数据库查询
- 服务器记录所有请求用于调试

## 停止服务
- **模拟服务器**: 在终端中按 `Ctrl+C`
- **主应用程序**: 关闭窗口或按 `Ctrl+C`

---
*生产部署时，请确保真实的身份证读卡器服务在端口7846上运行。*
