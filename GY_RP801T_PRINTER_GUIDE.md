# GY-RP801T打印机连接测试指南

## 概述

本文档提供了在无实物打印机情况下测试GY-RP801T打印机连接的完整解决方案。包含了模拟器、串口连接、ESC/POS指令生成等功能。

## 功能特性

### ✅ 已实现功能

1. **系统打印机检测**
   - 扫描Windows系统中已安装的打印机
   - 自动识别可能的GY-RP801T打印机

2. **串口连接管理**
   - 自动扫描可用串口
   - 支持USB转串口、虚拟串口
   - 连接状态监控和测试

3. **打印机模拟器**
   - 基于com0com虚拟串口的打印机模拟
   - 实时解析ESC/POS指令
   - 控制台输出模拟打印效果

4. **ESC/POS指令生成**
   - 完整的ESC/POS指令集支持
   - 文本格式化（字体、对齐、粗体等）
   - 条形码和二维码生成
   - 切纸、开钱箱等控制

5. **标签打印支持**
   - 标签尺寸设置
   - 文本、图形、条码混合打印
   - 自定义标签模板

6. **烟叶称重业务集成**
   - 称重小票模板
   - 称重标签模板
   - 业务数据格式化

## 环境要求

### 必需软件
- **Java 17+**: 运行环境
- **Maven**: 项目构建工具
- **Git Bash** (可选): 跨平台兼容

### 可选软件
- **com0com**: 创建虚拟串口对，用于模拟测试
- **GY-RP801T驱动**: 如果使用实物打印机

## 快速开始

### 1. 启动测试程序

#### Windows环境
```batch
# 双击运行
test-gy-rp801t.bat
```

#### Git Bash环境
```bash
# 运行脚本
./test-gy-rp801t.sh
```

### 2. 基本测试流程

1. **查找打印机**
   ```
   主菜单 → 1. 查找系统打印机
   ```

2. **扫描串口**
   ```
   主菜单 → 2. 扫描串口设备
   ```

3. **启动模拟器** (无实物打印机)
   ```
   主菜单 → 4. 启动打印机模拟器
   ```

4. **连接打印机** (有实物打印机)
   ```
   主菜单 → 3. 连接串口打印机
   ```

5. **测试打印功能**
   ```
   主菜单 → 5. 测试打印功能
   ```

## 详细使用说明

### 模拟器测试模式

如果没有实物打印机，推荐使用模拟器测试：

1. **安装com0com**
   - 下载并安装com0com
   - 创建虚拟串口对，如：CNCA0 ↔ CNCB0

2. **启动模拟器**
   ```
   主菜单 → 4. 启动打印机模拟器
   选择监听端口：CNCB0
   ```

3. **连接到模拟器**
   ```
   主菜单 → 3. 连接串口打印机
   选择连接端口：CNCA0
   ```

4. **测试打印**
   - 所有发送到CNCA0的数据会被CNCB0上的模拟器接收
   - 模拟器会在控制台显示解析的打印内容

### 实物打印机测试

如果有GY-RP801T实物打印机：

1. **连接打印机**
   - 使用USB数据线连接打印机
   - 或使用USB转串口线连接

2. **查找打印机端口**
   ```
   主菜单 → 2. 扫描串口设备
   ```

3. **连接打印机**
   ```
   主菜单 → 3. 连接串口打印机
   选择对应的串口
   选择波特率（通常115200或9600）
   ```

4. **测试连接**
   ```
   主菜单 → 6. 打印机状态
   ```

### 打印功能测试

程序提供多种打印测试：

1. **基本文本打印**
   - 测试基本的文本输出
   - 验证中文编码支持

2. **格式化文本测试**
   - 字体大小、粗体、下划线
   - 对齐方式测试

3. **条形码测试**
   - Code128条形码生成
   - 自定义条形码内容

4. **二维码测试**
   - QR码生成
   - 自定义二维码内容

5. **标签打印测试**
   - 标签模式打印
   - 混合内容布局

6. **ESC/POS指令测试**
   - 完整的烟叶称重小票
   - 业务数据格式化

### 烟叶称重业务测试

程序包含完整的烟叶称重业务场景：

```
主菜单 → 7. 烟叶称重测试
```

模拟数据包括：
- 烟农姓名
- 合同编号
- 叶片类型
- 称重重量
- 操作员信息

支持打印：
- 称重小票（ESC/POS格式）
- 称重标签（Label格式）

## 技术架构

### 核心组件

1. **WindowsSerialPrinter**
   - 串口连接管理
   - 数据发送/接收
   - 连接状态监控

2. **PrinterSimulator**
   - 打印机行为模拟
   - ESC/POS指令解析
   - 实时打印效果显示

3. **EscBuilder**
   - ESC/POS指令生成器
   - 链式调用API
   - 完整指令集支持

4. **LabelBuilder**
   - 标签打印指令生成器
   - 标签模式支持
   - 图形化布局

5. **GY_RP801T_PrinterTest**
   - 交互式测试程序
   - 功能集成测试
   - 用户友好界面

### 指令支持

#### ESC/POS指令
- `ESC @`: 重置打印机
- `ESC a`: 设置对齐方式
- `ESC E`: 设置粗体
- `ESC -`: 设置下划线
- `GS !`: 设置字体大小
- `GS k`: 打印条形码
- `GS V`: 切纸
- `GS (`: 二维码打印

#### 标签指令
- `SIZE`: 设置标签尺寸
- `GAP`: 设置标签间隙
- `CLS`: 清除缓冲区
- `TEXT`: 打印文本
- `BARCODE`: 打印条形码
- `QRCODE`: 打印二维码
- `BOX`: 绘制矩形
- `PRINT`: 执行打印

## 故障排除

### 常见问题

1. **无法找到串口**
   - 检查设备管理器中的串口
   - 确认驱动安装正确
   - 尝试不同的USB端口

2. **连接失败**
   - 检查端口是否被其他程序占用
   - 尝试不同的波特率
   - 确认串口参数设置

3. **打印无响应**
   - 检查打印机电源和连接
   - 验证串口线是否正常
   - 尝试发送测试指令

4. **中文显示乱码**
   - 确认使用GBK编码
   - 检查打印机中文字库支持
   - 尝试不同的编码方式

5. **模拟器无法启动**
   - 检查com0com是否正确安装
   - 确认虚拟串口对已创建
   - 检查端口名称是否正确

### 调试技巧

1. **启用详细日志**
   ```xml
   <!-- 在logback.xml中设置DEBUG级别 -->
   <logger name="com.tobacco.weight.hardware.printer" level="DEBUG"/>
   ```

2. **手动测试串口**
   ```java
   // 使用串口调试工具验证连接
   // 发送简单指令如: ESC @ (重置)
   ```

3. **检查依赖**
   ```bash
   mvn dependency:tree
   ```

## 扩展开发

### 添加新指令

1. **扩展EscBuilder**
   ```java
   public EscBuilder customCommand(byte... bytes) {
       write(bytes);
       return this;
   }
   ```

2. **扩展LabelBuilder**
   ```java
   public LabelBuilder customLabel(String command) {
       write(command.getBytes(StandardCharsets.UTF_8));
       return this;
   }
   ```

### 集成到主程序

将打印功能集成到主程序的PrinterManager中：

```java
// 在confirmWeighing方法中添加
if (printerManager != null && printerManager.isConnected()) {
    // 生成ESC/POS小票
    EscBuilder esc = new EscBuilder();
    byte[] receipt = esc.tobaccoReceipt(farmerName, contractNumber, 
                                       leafType, weight, operator).build();
    
    // 通过串口发送
    serialPrinter.write(receipt);
}
```

## 注意事项

1. **串口资源管理**
   - 程序退出时自动关闭串口
   - 避免多个程序同时占用串口

2. **编码兼容性**
   - 优先使用GBK编码支持中文
   - 提供UTF-8降级选项

3. **错误处理**
   - 完善的异常处理机制
   - 用户友好的错误提示

4. **性能优化**
   - 数据缓冲减少串口调用
   - 异步处理避免界面阻塞

## 总结

本解决方案提供了完整的GY-RP801T打印机测试环境，支持：

- ✅ 无需实物打印机即可完成开发测试
- ✅ 真实的ESC/POS指令生成和解析
- ✅ 完整的烟叶称重业务场景
- ✅ 用户友好的交互界面
- ✅ 详细的错误诊断和排除

通过模拟器和真实串口的双重支持，可以在任何环境下进行打印机功能的开发和测试。 