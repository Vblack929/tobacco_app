# 便携版打包使用说明

## 📦 概述

`build-portable-simple.bat` 是一个用于生成便携式程序包的批处理脚本。它将JavaFX应用程序打包成可以独立运行的便携式程序包，无需安装即可使用。

## 🎯 功能特点

- ✅ **一键打包**：自动完成编译、依赖复制、打包等步骤
- ✅ **便携部署**：生成的程序包可以复制到任何Windows机器使用
- ✅ **依赖完整**：包含所有必要的JavaFX依赖库
- ✅ **即插即用**：无需安装，双击即可运行
- ✅ **数据跟随**：数据库文件跟随程序包，便于备份和迁移

## 🚀 使用方法

### 1. 环境准备

确保系统已安装：
- **Java 17+**：用于编译和运行
- **Maven 3.6+**：用于项目构建
- **Windows 10/11**：目标操作系统

### 2. 执行打包

在项目根目录下运行：

```bash
build-portable-simple.bat
```

### 3. 打包过程

脚本会自动执行以下步骤：

1. **编译项目**：使用Maven编译Java源代码
2. **准备依赖**：复制JavaFX依赖库到lib目录
3. **创建便携包**：在target目录下创建"烟叶称重系统-便携版"文件夹
4. **生成启动脚本**：创建启动脚本和说明文件

### 4. 输出结果

打包完成后，会在 `target/烟叶称重系统-便携版/` 目录下生成：

```
烟叶称重系统-便携版/
├── tobacco-weight-windows-1.0.0.jar    # 主程序文件
├── lib/                                 # JavaFX依赖库目录
│   ├── javafx-controls-17.0.2-win.jar
│   ├── javafx-fxml-17.0.2-win.jar
│   ├── javafx-graphics-17.0.2-win.jar
│   └── javafx-base-17.0.2-win.jar
├── 启动烟叶称重系统.bat                 # 启动脚本
└── 使用说明.txt                         # 使用说明文件
```

## 📋 部署说明

### 目标机器要求

- **操作系统**：Windows 10/11
- **Java环境**：Java 17或更高版本
- **存储空间**：至少100MB可用空间
- **内存**：建议2GB以上

### 部署步骤

1. **复制程序包**
   - 将整个"烟叶称重系统-便携版"文件夹复制到目标机器
   - 可以复制到任意目录，如桌面或U盘

2. **检查Java环境**
   - 确保目标机器已安装Java 17+
   - 运行 `java -version` 验证

3. **启动程序**
   - 双击运行"启动烟叶称重系统.bat"
   - 程序会自动创建data目录和数据库文件

### 数据管理

- **数据库文件**：`data/tobacco_weight.db`
- **导出文件**：`exports/` 目录
- **日志文件**：`logs/` 目录

## 🔧 故障排除

### 常见问题

1. **编译失败**
   ```
   错误：编译失败！
   ```
   **解决方案**：
   - 检查Java版本是否为17+
   - 确保Maven正确安装
   - 检查网络连接（下载依赖需要网络）

2. **JAR文件未生成**
   ```
   错误：JAR文件未生成！
   ```
   **解决方案**：
   - 检查pom.xml配置
   - 清理target目录后重试
   - 查看Maven错误日志

3. **JavaFX依赖缺失**
   ```
   错误：JavaFX依赖复制失败！
   ```
   **解决方案**：
   - 检查网络连接
   - 清理Maven本地仓库缓存
   - 手动下载JavaFX依赖

4. **程序启动失败**
   ```
   程序启动失败！
   请检查Java版本是否为17或更高版本
   ```
   **解决方案**：
   - 检查Java版本：`java -version`
   - 确保JavaFX依赖完整
   - 查看详细错误信息

### 调试方法

1. **查看详细日志**
   ```bash
   # 在命令行中运行启动脚本，查看详细输出
   cd 烟叶称重系统-便携版
   启动烟叶称重系统.bat
   ```

2. **手动测试JAR文件**
   ```bash
   # 测试JAR文件是否可以正常运行
   java --module-path "lib\javafx-controls-17.0.2-win.jar;lib\javafx-fxml-17.0.2-win.jar;lib\javafx-graphics-17.0.2-win.jar;lib\javafx-base-17.0.2-win.jar" --add-modules javafx.controls,javafx.fxml,javafx.graphics -Dfile.encoding=UTF-8 -jar tobacco-weight-windows-1.0.0.jar
   ```

3. **检查文件完整性**
   ```bash
   # 检查所有必要文件是否存在
   dir tobacco-weight-windows-1.0.0.jar
   dir lib\*.jar
   ```

## 📊 性能优化

### 打包优化

1. **跳过测试**：使用 `-Dmaven.test.skip=true` 跳过测试
2. **跳过清理**：使用 `-Dmaven.clean.skip=true` 跳过清理步骤
3. **并行编译**：使用 `-T 1C` 启用并行编译

### 运行优化

1. **内存配置**：调整JVM内存参数
2. **垃圾回收**：选择合适的垃圾回收器
3. **启动时间**：优化依赖加载

## 🔄 版本管理

### 版本号规则

- **主版本号**：重大功能更新
- **次版本号**：功能增强和修复
- **修订版本号**：Bug修复

### 更新流程

1. **代码更新**：修改源代码
2. **版本更新**：更新pom.xml中的版本号
3. **重新打包**：运行打包脚本
4. **测试验证**：在目标环境测试
5. **发布部署**：分发新的程序包

## 📝 注意事项

### 安全考虑

- **数据备份**：定期备份data目录
- **权限管理**：确保程序有写入权限
- **网络安全**：避免在不安全网络中使用

### 兼容性

- **Java版本**：确保使用Java 17+
- **操作系统**：仅支持Windows 10/11
- **硬件要求**：建议2GB内存以上

### 维护建议

- **定期更新**：及时更新Java和依赖库
- **日志清理**：定期清理日志文件
- **数据备份**：定期备份重要数据

## 📞 技术支持

如遇到问题，请：

1. **查看日志**：检查logs目录下的日志文件
2. **查阅文档**：参考README.md和本说明
3. **提交Issue**：在项目仓库提交Issue
4. **联系开发**：通过邮件或GitHub联系开发者

---

**最后更新**：2025-07-27  
**版本**：v1.0.0  
**作者**：开发团队 